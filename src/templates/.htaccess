# Disable directory browsing
Options -Indexes
# Hide the contents of directories
IndexIgnore *

# Deny access to files with extensions .php
<FilesMatch "\.(php)$">
Order allow,deny
Deny from all
</FilesMatch>